global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 2m
  receiver: 'jenkins-rollback-webhook'

receivers:
  - name: 'jenkins-rollback-webhook'
    webhook_configs:
      - url: 'http://172.31.95.212:8080/generic-webhook-trigger/invoke?token=ROLLBACK_11F98E_TOKEN'
        http_config:
          basic_auth:
            username: 'admin'
            password: '113a140b62b47191422f2303c3461cf222'
        send_resolved: false
